# nc_py_api[app] pulls in FastAPI + uvicorn[standard] for the ExApp runtime.
# Pinned to 0.30.x: the async migration in app/main.py targets this API.
# 0.31.x removes the deprecated sync TalkBot entirely and may shift APIs —
# bump deliberately, not implicitly. An unpinned `>=0.21.0` is what pulled
# 0.30.1 into the build and broke the sync enabled_handler.
nc_py_api[app]>=0.30,<0.31

# OpenAI SDK — wired into adapters/openai_adapter.py.
openai>=1.40.0

# Local dev convenience: load .env when running outside the AppAPI container.
python-dotenv>=1.0.0

# --- Fase 2: RAG -----------------------------------------------------------
# httpx: cliente REST read-only contra Supabase Storage (adapters/
# supabase_storage_loader.py). Ya entra como transitiva de openai/nc_py_api;
# se pinea explícito porque ahora es dependencia directa.
httpx>=0.27,<0.29
# psycopg[binary] + pgvector: cliente async del vector store (ADR-007).
psycopg[binary]>=3.1,<4
pgvector>=0.3,<0.5
# tiktoken: tokenizador cl100k_base para el chunking (ADR-009). Solo se usa en
# el adapter; el dominio no lo importa.
tiktoken>=0.7,<1
# pypdf: extracción de texto de PDF en adapters/pdf_text_extractor.py
# (ADR-006-ter). Solo lo usa el adapter; services/ y domain/ no lo importan.
pypdf>=4,<6

# --- Bloque 3: dashboard_db (MySQL read-only, ADR-020/022) -----------------
# Driver MySQL async que usa DashboardMySQLAdapter. Import PEREZOSO en el adapter
# (solo si `dashboard_ready`), así que no lastra los despliegues que no usan el
# dashboard. Cython + wheels precompilados; el build del contenedor lo instala.
asyncmy>=0.2.9,<0.3
# asyncmy NO declara `cryptography` (Requires vacío), pero la necesita en RUNTIME
# para el full-auth de caching_sha2_password/sha256_password de MySQL 8 (cifrado RSA
# de la contraseña sobre la conexión NO-TLS que sale por el túnel SSH). Sin ella, la
# conexión falla con "'cryptography' package is required". Es el ÚNICO extra de auth
# (no hace falta nada más para el handshake en el camino localhost del túnel).
cryptography>=42,<44